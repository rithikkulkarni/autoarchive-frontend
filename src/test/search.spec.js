// visit /
// type “tesla”
// click a card
// verify /model/<id> loads and chart canvas exists

import { describe } from "vitest";
import { describe, it} from "vitest";

describe.skip("lib/api.js", () => {
  it("does something", () => {
    // skipped
  });
});